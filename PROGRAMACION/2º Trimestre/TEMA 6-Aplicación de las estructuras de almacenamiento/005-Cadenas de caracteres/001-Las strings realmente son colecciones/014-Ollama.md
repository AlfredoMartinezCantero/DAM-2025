# Descargamos la instalacion de ollama en la máquina virtual

sudo apt install curl (si no tenéis curl)

curl -fsSL https://ollama.com/install.sh | sh

# Comprobar la versión instalada

ollama --version