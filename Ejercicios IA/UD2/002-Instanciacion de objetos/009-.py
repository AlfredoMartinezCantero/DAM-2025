#En este ejercicio vamos a ver el uso de `import`, este sirve para traer partes de código externos (ya sean de la biblioteca estándar de Python o módulos que nosotros creemos) para poder usar sus funciones.
#`math.sqrt` es una función del módulo `math` de Python, el cual calcula la raíz cuadrada de un número, en este caso, `(64)`.
#Almacenamos los valores `(10, 20, 30)` en el objeto llamado `mi_tupla`.
Después usaremos  `print` para mostrar por pantalla los resultados.

```
import math
resultado_raiz = math.sqrt(64)

print(resultado_raiz)

mi_tupla = (10,20,30)
print(mi_tupla)
```
