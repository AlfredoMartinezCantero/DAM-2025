En la máquina virtual:
Acceder a la tienda de app (Centro de aplicaciones)

Apache NetBeans
openJDK

Terminal:
sudo apt install openjdk-17-jdk

En NetBeans:

File - New porject - Java with Ant - next - Project Name: PrimerPrograma (ha de empezar por mayúscula) - create main class

package primerprograma; // Este archivo pertenece a un paquete, que es un proyecto

public class PrimerPrograma { // Java es un lenguaje estrictamente orientado a objetos
                                // Lo que quiere decir que, lo quieras o no, vas a tener una clase
    
    public static void main(String[] args) { //Esta es funcion principal
            // TODO code application logic here
            System.out.println("Hola mundo en Java");   // Esto es una salida en Java
    }
}